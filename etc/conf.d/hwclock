# /etc/conf.d/hwclock

# Set to "YES" to set the system clock from the RTC on boot.
clock_hctosys="YES"

# Set your hardware clock's timezone here.
# For a hardware clock storing UTC time (recommended), use:
# clock_opts="--utc"
#
# For a hardware clock storing local time (for dual-booting with Windows), use:
# clock_opts="--localtime"
#
clock_opts="--utc"
