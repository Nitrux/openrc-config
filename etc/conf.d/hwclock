# Save the system clock to the hardware clock on shutdown?
clock_systohc="YES"

# Hardware clock is in UTC or local time.
# To use UTC, set clock_opts="--utc"
# To use local time, set clock_opts="--localtime"
clock_opts="--utc"
